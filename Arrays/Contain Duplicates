# Problem: Contain Duplicates
# Link: https://leetcode.com/problems/contains-duplicate

#BRUTE
def contains_duplicates(nums):
    n = len(nums)
    for i in range(0,n):
        for j in range(i + 1, n):
            if nums[i] == nums[j]:
                return True
    return False
nums= [1,2,3,1]
print("Contains duplicates:", contains_duplicates(nums))

#OPTIMAL(USING HASHSET)
def contains_duplicates(nums):
    n = len(nums)
    seen = set()
    for i in range(0,n):
        if nums[i] in seen:
            return True
        else:
            seen.add(nums[i])
    return False   
